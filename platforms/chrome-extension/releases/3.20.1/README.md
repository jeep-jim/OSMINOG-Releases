# OSMINOG Chrome 3.20.1 — Layout Rollback Hotfix

Emergency forward-version recovery from the broken 3.20.0 package. The complete published 3.19.12 package was verified by SHA-256 and reused byte-for-byte except for `manifest.json` and `OSMINOG_BUILD.json`, whose version metadata was raised to 3.20.1. This restores the working application shell, styles and Shared Vision runtime while allowing Dev Hub to update forward.
