# Chrome extension

Chrome extension packages. Development updates use OSMINOG Dev Hub; production delivery uses Chrome Web Store.

Every version must live under `releases/<version>/` with a complete ZIP, release manifest, SHA-256, fixed extension identity verification and validation state.
