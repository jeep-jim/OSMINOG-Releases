# Web + PWA

Website and Progressive Web App deployments. Packages may be deployment manifests or immutable build archives.

Every version must live under `releases/<version>/` with a full package, release manifest, SHA-256 and validation state.
