# Public verification keys

Only public verification keys belong here. Private signing material must never be committed.

Active key: `osminog-release-2026-01`.

The private key is stored outside this public repository and must be installed as a protected GitHub Actions secret before automatic signing is enabled.
