# Update channels

- `dev` — rapid owner and internal development builds.
- `beta` — wider pre-release validation.
- `stable` — production-approved builds.

A feed may advance only after the complete remote artifact exists and its byte size and SHA-256 have been verified.
